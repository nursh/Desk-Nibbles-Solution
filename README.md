### Desk Nibbles Solution

* This is a solution to Desk Nibbles problem set
* To install and run program:
  * ```
      npm install
    ```
  * ```
      npm start
    ```
* The application is running on port:3000